class Bayes

def __init__(hypothesis, priors, observations, likelihood):
    self.hypothesis = hypothesis
    self.priors = priors
    self.observations = observations
    self.likelihood_array = likelihood_array
